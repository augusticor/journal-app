import React from 'react';
import ReactDOM from 'react-dom';

import JournalApp from './JournalApp';
import './styles/styles.scss';

const rootComponent = document.getElementById('root');

ReactDOM.render(<JournalApp />, rootComponent);
